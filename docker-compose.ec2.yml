services:
  api:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    command: node index.js
    restart: unless-stopped
